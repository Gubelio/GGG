political_decisions = {

    embrace_shamush = {
	picture = "embrace_shamush"
        potential = {
            is_culture_group = peninsular_diwvo
			NOT = { has_country_flag = we_embraced_shamush }
			POU = { government = theocracy }
        }

        allow = {
			NOT = { in_sphere = POU }
			OR = {
				is_secondary_power = yes
				is_greater_power = yes
			}
			prestige = 10
			romanticism = 1
        }

        effect = {
			set_country_flag = we_embraced_shamush
			clr_country_flag = wompowushan_country
			set_country_flag = we_are_shamush
			religion = shamush
			prestige = 5
			relation = {
				who = POU
				value = -200
			}
			POU = { country_event = { id = 50034 } }			
        }
		
		ai_will_do = {
			factor = 1
			modifier = { 
				factor = 0 #will never do if
				has_global_flag = poumnia_defeated_shamush #poumnia has defeated a previous shamush dissident state
			}
			modifier = { 
				factor = 0 #will never do if
				relation = { POU = THIS value = 50 } #relations 50 or over with POU
			}
		}
		
    }
	
    embrace_shamush_peaceful = { #If poumnia is not a theocracy anymore pruuntish peoples can peacefully become shamush
	picture = "embrace_shamush"
        potential = {
            is_culture_group = peninsular_diwvo
			NOT = { has_country_flag = we_embraced_shamush }
			NOT = { POU = { government = theocracy } }
        }

        allow = {
			NOT = { in_sphere = POU }
        }

        effect = {
			set_country_flag = we_embraced_shamush
			clr_country_flag = wompowushan_country
			set_country_flag = we_are_shamush
			religion = shamush
			prestige = 5	
        }
		
		ai_will_do = {
			factor = 1
		}
		
    }
	
	
	
}